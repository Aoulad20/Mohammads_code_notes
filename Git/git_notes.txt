Initializing a new repository: git init:-

cd /path/to/your/existing/code 
git init

Cloning an existing repository: git clone:-

git clone <repo url>

To add and commit files to a Git repository:-

git add --all

git commit -m "updated with the  folders of notes"